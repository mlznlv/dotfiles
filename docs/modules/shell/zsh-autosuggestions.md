# Zsh autosuggestions

`shell.zsh.autosuggestions` requests `zsh-autosuggestions` from Homebrew on
macOS and mise on Debian. It depends on `shell.zsh`.

This release exposes static ownership intent and resolution only. It does not
install the package, add shell startup files, plan, or apply changes.
