# Starship

`prompt.starship` is the primary prompt module. It requests `starship` from
Homebrew on macOS and as a mise tool on Debian, and depends on `shell.zsh`.

Only discovery and resolution are released. Installation, configuration,
provider observation, planning, and apply remain unavailable.
